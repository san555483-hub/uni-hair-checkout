services:
  - type: web
    name: uni-hair-checkout
    env: node
    buildCommand: pnpm install --frozen-lockfile=false && pnpm build
    startCommand: node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
